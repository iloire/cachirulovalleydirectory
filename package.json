{
	"name": "cachirulovalleydirectory",
	"version": "0.0.4",
	"scripts": {
		"test": "type redis-server >/dev/null 2>&1 || { echo >&2 \"I require redis-server but it's not installed.  Please install redis server.\"; exit 1; }; echo '\nlaunching redis-server in background with testing config...\n'; redis-server test/redis.conf; node test/runall.js; kill `cat redis.pid`"
	},
	"engines": {
		"node": ">= 0.4.0"
	},
	"private": true,
	"repository": {
		"type": "git",
		"url": "https://github.com/iloire/cachirulovalleydirectory"
	},
	"dependencies": {
		"express": "2.5.0",
		"ejs": ">= 0.0.1",
		"redis": "0.7.x",
		"oauth": "0.9.x",
		"linkedin-js": ">= 0.1.4",
		"reds": ">= 0.1.1",
		"async": ">= 0.1.15",
		"connect-redis": ">= 1.2.0",
		"gzippo": "0.1.x"
	}
	,
	"devDependencies": {
		"Faker": ">= 0.1.3",
		"zombie": "0.12.x"
	}
}